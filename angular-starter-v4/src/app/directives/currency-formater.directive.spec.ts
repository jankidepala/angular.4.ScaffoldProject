import { CurrencyFormaterDirective } from './currency-formater.directive';

describe('CurrencyFormaterDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrencyFormaterDirective();
    expect(directive).toBeTruthy();
  });
});
